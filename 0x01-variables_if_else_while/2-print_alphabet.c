#include <stdio.h>
/**
 * main - print lower case 
 * Return: Always 0 (pass)
 */
Int main(void)
 {
char ch;
for(ch = 'a';
	while       
		(ch <= 'z'){
	       putchar(ch);	
		ch++;
		}
	return (0);
}
